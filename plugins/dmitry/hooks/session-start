#!/usr/bin/env bash
set -euo pipefail

STATE_FILE="${TMPDIR:-${TEMP:-/tmp}}/dmitry-pretool-state"
GREP_STATE="${TMPDIR:-${TEMP:-/tmp}}/dmitry-pretool-grep-state"
rm -f "$STATE_FILE" "$GREP_STATE"
echo '{}'
